{
  "build": {
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && pip install -r requirements.txt && gunicorn app:app",
    "restartPolicyType": "ON_FAILURE"
  }
}
